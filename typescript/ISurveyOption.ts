/**
 * Interface for survey question options.
 * Used in survey-option-card component and survey screen.
 */
export interface ISurveyOption {
  emoji: string;
  label: string;
  description: string;
}
